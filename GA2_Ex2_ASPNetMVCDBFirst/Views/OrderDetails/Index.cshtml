@model IEnumerable<GA2_Ex2_ASPNetMVCDBFirst.OrderDetail>

@{
    ViewBag.Title = "Order Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="card p-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2 class="mb-0">Order Details</h2>
            <a href="@Url.Action("Create")" class="btn btn-primary">Create New Order Detail</a>
        </div>
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Quantity)</th>
                        <th>@Html.DisplayNameFor(model => model.UnitAmount)</th>
                        <th>@Html.DisplayNameFor(model => model.Item.ItemName)</th>
                        <th>@Html.DisplayNameFor(model => model.Order.OrderID)</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.Quantity)</td>
                            <td>@Html.DisplayFor(modelItem => item.UnitAmount)</td>
                            <td>@Html.DisplayFor(modelItem => item.Item.ItemName)</td>
                            <td>@Html.DisplayFor(modelItem => item.Order.OrderID)</td>
                            <td>
                                <a href="@Url.Action("Edit", new { id = item.ID })" class="btn btn-sm btn-outline-primary">Edit</a>
                                <a href="@Url.Action("Details", new { id = item.ID })" class="btn btn-sm btn-outline-secondary">Details</a>
                                <a href="@Url.Action("Delete", new { id = item.ID })" class="btn btn-sm btn-outline-danger">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

